/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.coingecko.com','storage.googleapis.com','s2.coinmarketcap.com'], 
      },
};

export default nextConfig;
